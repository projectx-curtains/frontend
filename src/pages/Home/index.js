import React from "react";
import Banner from "./Banner";
import Categories from "./Categories";

const Home = (props) => {
  return (
    <main className="home">
      <Banner />
      <Categories />
    </main>
  );
};

export default Home;
